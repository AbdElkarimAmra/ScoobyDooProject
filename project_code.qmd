---
title: "Project Milestone 2"
author: "Karim Amra, Makol Chuol, Sarah Radzilowski, Noah Steinhouse"
date: now
date-format: "YYYY-MM-DDTHH:mm:ssZ"
format:
  html:
    toc: true
    toc-depth: 2
    embed-resources: true
    code-tools: true 
    df-print: paged
---

# Load Data/Packages

```{r}
# load data
library(tidyverse)
library(tidytuesdayR)
library(patchwork)

scoobydoo <- readr::read_csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/main/data/2021/2021-07-13/scoobydoo.csv')
```

```{r}
# make "guest" dataset
scoobydoo_guest <- scoobydoo |>
  select(batman, blue_falcon, hex_girls, scooby_dum, scrappy_doo, 
         series_name, title, imdb, engagement) |>
  pivot_longer(cols = c(batman, blue_falcon, hex_girls, scooby_dum, scrappy_doo), 
               names_to = "character", values_to = "TF") |>
  mutate(imdb = as.numeric(imdb)) 

```

# Data Viz and Summary
```{r}
# how many times to characters appear?
scoobydoo |>
  select(batman, scrappy_doo, scooby_dum, hex_girls, blue_falcon) |>
  pivot_longer(cols = everything(), names_to = "character", values_to = "yn") |>
  group_by(character, yn) |>
  summarize(n = n()) |>
  pivot_wider(names_from = yn, names_prefix = "Appear_",
              values_from = n) |>
  arrange(Appear_TRUE)
```


```{r}
# boxplot of ratings (w/ or w/o guest)
scoobydoo_guest |>
  ggplot(aes(x= TF, y=imdb, fill = character))+
  geom_boxplot()+
  facet_grid(~character)+
  theme(legend.position = "bottom")+
  labs(title = "Comparison of IMDB Ratings for Episodes with and without Guest Characters",
       x = "Appears? (F/T)",
       y = "IMDB Score (0-10)")


# summarize median
scoobydoo_guest |>
  filter(imdb != "NULL") |>
  group_by(character, TF) |>
  summarize(median = median(imdb)) |>
  pivot_wider(names_from = TF, names_prefix = "Appears_", 
              values_from = median)
```

```{r}
# calculating median, mean and sd for imdb, engagement
scoobydoo |>
  select(imdb, engagement) |>
  filter(imdb != "NULL",
         engagement != "NULL") |>
  mutate(imdb = as.numeric(imdb),
         engagement = as.numeric(engagement)) |>
  summarize(median_imdb = median(imdb),
            mean_imdb = mean(imdb),
            sd_imdb = sd(imdb),
            median_engagement = median(engagement),
            mean_engagement = mean(engagement),
            sd_engagement = sd(engagement)) |>
  pivot_longer(cols = everything(),
               names_to = c("stat", "variable"),
               names_sep = "_") |>
  pivot_wider(names_from = stat, values_from = value) |>
  mutate(mean = round(mean, 2),
         sd = round(sd, 2))
```

```{r}
# box plot for IMDB score distribution
scoobydoo |>
  filter(imdb != "NULL") |>
  ggplot(aes(x=as.numeric(imdb)))+
  geom_boxplot()+
  labs(x = "IMDB Score",
       title = "Distribution of IMDB scores")
```

 
```{r}
# distribution of imdb, engagement
scoobydoo |>
  filter(engagement != "NULL") |>
  ggplot(aes(x=as.numeric(engagement)))+
  geom_histogram()+
  labs(title = "Distribution of Engagement (Normal Scale)",
       x= "Engagement")

scoobydoo |>
  filter(engagement != "NULL") |>
  ggplot(aes(x=as.numeric(engagement)))+
  geom_histogram()+
  scale_x_log10()+
  labs(title = "Distribution of Engagement (Log Scale)",
       x = "log(Engagement)")


```


```{r}
# bar plot of episodes guest appears in
scoobydoo_guest |>
  ggplot(aes(x=character, fill=TF))+
  geom_bar(position = "dodge")+
  labs(title = "Number of Episodes a Character is in",
       y = "Number of Episodes",
       fill = "Appears?")
```



```{r}
# missing entries
scoobydoo |>
  select(imdb) |>
  filter(imdb == "NULL") |>
  count(imdb) 

scoobydoo |>
  select(engagement) |>
  filter(engagement == "NULL") |>
  count(engagement)
```



# Models
```{r}
# keep only valid (non-missing) IMDB and recode variables
model_data <- scoobydoo %>%
  filter(imdb != "NULL") %>%
  mutate(
    imdb = as.numeric(imdb),
    batman = as.numeric(batman),
    blue_falcon = as.numeric(blue_falcon),
    hex_girls = as.numeric(hex_girls),
    scooby_dum = as.numeric(scooby_dum),
    scrappy_doo = as.numeric(scrappy_doo)
  )

```

```{r}

# Run linear regression with guest characters as predictors
model <- lm(imdb ~ batman + blue_falcon + hex_girls + scooby_dum + scrappy_doo, data = model_data)

```

```{r}

# Summarize regression results (shows coefficients, model fit, significance)
summary(model)
```

# Confidence Interval

```{r}

# Obtain confidence intervals for the coefficients (for inference)
confint(model)

```

```{r}
# Model diagnostics: R-squared and plots
summary(model)$r.squared     # Shows variance explained
par(mfrow=c(2,2))
plot(model)                  # Visualize residuals and check assumptions

```

